# 2026-09-11 工作日志

## 人判新契约落地收尾 + 全链路测试

- 前序已把 6 份 docs 对齐新人判契约、2D 页面（classroom.html/index.html）清掉全部假指标；
  本日把最后一项 #291（HTTP 契约测试）跑通并补回 6 个非 HTTP 测试全绿。
- **测试结果（全绿）**：
  - HTTP 端到端 `tools/test_http_session.mjs`：21 项断言全过（隔离端口 8137）。
  - 非 HTTP：`test_session` / `test_summary` / `test_skillcards` / `test_integration` / `test_journal` / `test_viz_render` 全过（viz_render 35 passed）。
  - 整条回归链 **0 失败**。
- **重要坑：8080 端口有残留旧服务进程在跑旧代码**（net 探测 PORT_8080_OPEN）。
  第一次 HTTP 测试因此打到旧 teacher.js，3 项断言失败（gains 缺 answered/open、仍含 completeness/finalP）。
  → 改用隔离端口 8137 跑全新服务即全过。**用户若要用 8080 须先重启服务以加载新代码。**
- **#293 标定报告**：`lingjing/.workbuddy/reports/2026-09-11-停止给AI学生打分.md`
  复现已删的 2-gram「接住」判定，对 9 组（学生探测/教师回答/人判）真实算出分数，
  Pearson r = -0.288（≈0 且为负），证明该判定是噪声 → 正式停给 AI 学生打分。

## 红线（全程遵守）
- 本次全部改动**未提交、未推送**远程；是否提交 / 发版 / 改许可证待用户拍板。
- 密钥只走环境变量 `LINGJING_OR_KEY`，不入库。
- 未擅自删除 teaching.js 中「已停用占位」函数（initP / fallbackUnderstand / teachingEffect / classEntropy / addressScore / overlapScore），待用户拍板是否彻底清理。

## 待用户拍板
1. 是否提交并推送本次人判契约改动。
2. 是否发版 / 改许可证（UNLICENSED→开源+商用双轨）。
3. 是否彻底删除 teaching.js 已停用占位函数。
4. 重启 8080 服务以加载新代码（或确认用新端口）。

## 用户当场反馈（工作方式与定位，必须记牢）
- **定位红线（用户原话重申）**：机器学生只是工具/镜子，**根本不会学习**，我们服务的是**人类使用者**；
  给他们打分既无意义也是假指标。本契约已贯彻——产物里无任何"学生理解度/接住率"分数。
- **工作方式（用户 2026-09-11 明确批评）**：不要边写边测、不要每步都跑测试——**所有改动一次性写完，
  最后统一跑一次全测试即可**。前序拆成多次测试拖长了时间，已纠正。
